name: familybudgetsash
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2+1
  path: ^1.9.1
  path_provider: ^2.1.5
  google_fonts: ^8.1.0
  fl_chart: ^1.2.0
  csv: ^8.0.0
  share_plus: ^10.0.0
  file_picker: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
